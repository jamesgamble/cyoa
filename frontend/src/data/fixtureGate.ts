/**
 * Sample content is for tests and local development only. Production
 * builds (`vite build`) never ship or display it — every public page
 * reads real data from the API instead.
 */
export const FIXTURES_ENABLED: boolean = !import.meta.env.PROD;
